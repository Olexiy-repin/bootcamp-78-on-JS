/*
 * Властивості елемента (hero)
 * - Зображення
 * - Текст та textContent
 * https://images.pexels.com/photos/1870376/pexels-photo-1870376.jpeg?auto=compress&cs=tinysrgb&dpr=2&w=480
 */

// const refs = {
//   mainTitle: document.querySelector('.js-hero-title'),
//   img: document.querySelector('.js-hero-image'),
// };

// console.log(refs);

// console.log(refs.mainTitle.textContent);

// refs.mainTitle.textContent = 'My new title';

// refs.img.alt = 'red cat';
// refs.img.src =
//   'https://images.pexels.com/photos/1870376/pexels-photo-1870376.jpeg?auto=compress&cs=tinysrgb&dpr=2&w=480';
// refs.img.height = 213;

/*
 * Атрибути
 * - get(ім'я-атрибута)
 * - set(ім'я-атрибута, значення-атрибута)
 * - remove(ім'я-атрибута)
 * - has(ім'я-атрибута)
 */

// console.log(refs.img.getAttribute('alt'));
// console.log(refs.img.alt);
// console.log(refs.img.getAttribute('src'));
// console.log(refs.img.src);

// refs.img.setAttribute('alt', 'red cat');
// refs.img.alt = 'red cat';

// refs.img.removeAttribute('alt');
// refs.img.removeAttribute('src');

// console.log(refs.img.hasAttribute('alt'));
// console.log(refs.img.hasAttribute('src'));
// console.log(refs.img.hasAttribute('height'));
